define(['component/_estadisticasMasterComponent'],function(_EstadisticasMasterComponent) {
    App.Component.EstadisticasMasterComponent = _EstadisticasMasterComponent.extend({
		postInit: function(){
			//Escribir en este servicio las instrucciones que desea ejecutar al inicializar el componente
		}
    });

    return App.Component.EstadisticasMasterComponent;
});