define(['component/_foroMasterComponent'],function(_ForoMasterComponent) {
    App.Component.ForoMasterComponent = _ForoMasterComponent.extend({
		postInit: function(){
			//Escribir en este servicio las instrucciones que desea ejecutar al inicializar el componente
		}
    });

    return App.Component.ForoMasterComponent;
});